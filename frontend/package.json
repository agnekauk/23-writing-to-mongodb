{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "common.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "dead-server --port=3006 --host=localhost"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.6"
  },
  "dependencies": {
    "cors": "^2.8.5"
  }
}
